# Auth API (BFF proxies to this URL server-side)
NUXT_PUBLIC_AUTH_API_URL=http://localhost:5001

# BFF access-token sessions use Nitro storage `bffSessions` (default: memory).
# For multiple Node instances, switch nuxt.config nitro.storage.bffSessions to driver `redis` and set URL via Nitro docs.
